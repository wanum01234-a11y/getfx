# Frontend development env (Vite)
# If you want the Settings fallback webhook URL to show the live domain even on localhost,
# set this to your production domain.
VITE_PUBLIC_BASE_URL=https://getfxpro.space

# Local API server target for Vite dev proxy
VITE_API_PROXY_TARGET=http://localhost:8787
